<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Invoice - Pesticide Supply System</title>
    <link rel="stylesheet" href="/css/cart.css">
</head>
<body>
    <div class="receipt-container">
        <h1>Invoice</h1>
        <p>Thank you for your order!</p>
        <div>
            <strong>Name:</strong> <%= order.name %><br>
            <strong>Email:</strong> <%= order.email %><br>
            <strong>Delivery Address:</strong> <%= order.address %><br>
            <strong>Order ID:</strong> <%= order.id %><br>
            <strong>Date:</strong> <%= order.created_at ? order.created_at.toLocaleString ? order.created_at.toLocaleString() : order.created_at : '' %><br>
        </div>
        <div class="receipt-details">
            <h2>Order Summary</h2>
            <ul>
                <% items.forEach(item => { %>
                    <li>
                        <strong>Product:</strong> <%= item.product %> <br>
                        <strong>Quantity:</strong> <%= item.quantity %> <br>
                        <strong>Price:</strong> $<%= item.price.toFixed(2) %> <br>
                    </li>
                <% }); %>
            </ul>
            <h3>Total: $<%= order.total.toFixed(2) %></h3>
            <p style="margin-top: 1em; color: #555;">
                <em>
                    Please note: Payment will be collected after delivery. A physical receipt will be provided at that time.
                </em>
            </p>
        </div>
        <button onclick="window.print()">Download Invoice</button>
    </div>
</body>
</html>